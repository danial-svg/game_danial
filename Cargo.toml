[package]
name = "game"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.19.0", features = ["mp3", "vorbis"] }
# اگر پکیج‌های دیگری هم دارید اینجا می‌مانند، اما خط دیگری برای bevy نباید باشد.